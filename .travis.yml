language: java

cache:
  directories:
  - .autoconf
  - $HOME/.m2

services:
  - mysql
before_install:
  - mysql -e 'SET GLOBAL lower_case_table_names=1'
  - mysql -e 'DROP DATABASE IF EXISTS market;'
  - mysql -e 'CREATE DATABASE market;'

script: mvn test -Dspring.profiles.active=mysql